name: 'heymax_eventstream_data'
version: '1.0.0'

profile: 'heymax_eventstream_data'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  heymax_eventstream_data:
    example:
      +materialized: view
